# تحقق النشر للقراءة فقط — 21 سبتمبر 2026

يستكمل هذا المستند [مطابقة المصدر](2026-09-21-independent-source-verification.md) قبل إصلاح التطبيق. نافذة السجلات: **2026-09-20 10:44:12 UTC إلى 2026-09-21 10:44:12 UTC**، مستندة إلى ساعة قاعدة الإنتاج؛ ساعة بيئة التنفيذ المحلية ليست مرجع هذه النافذة. البيانات العددية الآمنة في [ملف الأدلة](2026-09-21-independent-hosted-evidence.json).

## نقاط الصحة وهوية النشر

| البيئة | /version | /health | /health/ready | المخطط |
|---|---|---|---|---|
| الإنتاج `https://dawaee-api.onrender.com` | 200، `e59f864a6ae32ec28276b1b786e64b0621506308` | 200/ok | 200/ready | `0095_verified_email_registration.sql` |
| المعاينة `https://dawaee-audit-preview.onrender.com` | 200، `3c5969a9d7a4ad125bb49c43ad391c4fdec1e39c` | 200/ok | 200/ready | `0095_verified_email_registration.sql` |

المشاهدة في 10:42:52–10:42:55 UTC. كلا `/version` يعرض version وbuiltAt بقيمة unknown؛ الـcommit والمخطط متاحان. لا تُعد هذه الطلبات رحلة تسجيل/جرعة أو تجربة جهاز. readiness لا يغطي push-receipts في المصدر الحالي (N7).

Render أكد أن الإنتاج API والعامل مربوطان بفرع `fix/ios-patient-feedback-20260918`، والنشر التلقائي لكليهما off. العامل: `dep-dao35lo473hc73b8rs30` live منذ 2026-09-20 19:26:18 UTC؛ API: `dep-dao37c3m8hqs73d3pqgg` live منذ 19:29:44 UTC؛ كلاهما e59. أي أن ترتيب النشر السابق كان العامل ثم API. المعاينة `dep-dao2epfavr4c73asasv0` على 3c5969a منذ 18:37:35 UTC، ونشرها التلقائي على فرعها الحالي مفعّل؛ لم ندفع إلى ذلك الفرع.

هذه ليست مطابقة commit بين المعاينة والإنتاج، حتى مع تماثل شجرة التطبيق. لا تُستعمل لتجاوز شرط المستخدم الجديد بالنشر والتحقق على **الـcommit نفسه**.

## قاعدة الإنتاج والترحيلات والعامل

استعلامات Supabase جرت داخل `BEGIN READ ONLY` ولم تقرأ صفوف مستخدمين أو tokens أو محتوى سريري.

- PostgreSQL **17.6**؛ 95 سجلًا في `public.schema_migrations`، وليس سجل ترحيلات Supabase الخاص.
- قورنت أسماء الملفات والـMD5 الـ95 **ملفًا بملف** مع المرشح: لا ناقص ولا مختلف ولا زائد.
- بصمة السجل المرتب، مفصولًا بـnewline دون newline أخير: `dd07cc3908d47d2a0fc89a10b9ea81b4`. مع newline أخير: `dfff424c087fe4704ff7c7c8cbb5e66b`. اختلاف هاتين الصيغتين ليس اختلاف migration.
- عدد جداول public ذات RLS وFORCE RLS: **33**. هذا فحص إعداد قراءة فقط، وليس تشغيل مسبار RLS الكتابي على الإنتاج.
- آخر سجل لكل المهام الثماني ناجح وعلى e59، بما فيها push-receipts وhousekeeping. سبع مهام لها 1440 تشغيلًا لكل منها في النافذة؛ housekeeping له 24. **10104 تشغيلًا مسجلًا، صفر فشل مسجل**. النافذة تشمل commit السابق 63b5b8d ثم e59؛ آخر كل مهمة e59.
- سجلات العمليات منفصلة عن job_runs: ظهر فشل مصادقة اتصال واحد للعامل عند 19:26:17 UTC وقت النشر، لا يوجد له job_run لأن الاتصال يفشل قبل إنشاء سجل المهمة. لذلك «صفر فشل مسجل» لا تعني «لم يحدث خطأ بالعامل».

قُرئت صلاحيات الأدوار: app وworker بلا superuser/bypassrls/createrole/createdb؛ owner بلا superuser/bypassrls لكنه يملك createrole. ظهر اتصالان لـdawaee_app واتصالان لـdawaee_worker، وصفر اتصال owner في العينة. هذه قرينة تشغيل قوية على الأدوار المحدودة، لكنها لا تثبت غياب بيانات اعتماد owner من **بيئة API**؛ ولا تتيح pg_stat_activity وحدها نسب كل اتصال إلى خدمة بعينها.

## سجل المعاينة وحدود الوصول

محاولتا قراءة `schema_migrations` عبر موصل Render للقاعدة المعروفة انتهتا بـunexpected EOF و`SSL/TLS required`. لم تُغيّر إعدادات TLS أو الشبكة أو الأسرار لتجاوز الخطأ. بناء عليه:

- رقم المخطط من `/version` مثبت، وready=200 مثبت.
- **المقارنة المستقلة لكل checksum في قاعدة المعاينة غير متحققة في هذه الجولة**.
- **job_runs للمعاينة خلال 24 ساعة وcommit العامل من metadata غير متحققين مباشرة**. توجد سجلات worker في خدمة المعاينة، ولكنها لا تحمل commit يمكن الاعتماد عليه بدل جدول job_runs.

## API والعامل: سجلات 24 ساعة

قُرئت جميع صفحات **error/warn الخاصة بسجلات app** في النافذة، مع مجموعات نجاح العمل ذات items>0. لم تُنسخ معرفات الطلبات أو العناوين أو الرسائل الخام إلى المستودع. لم نقرأ كل سطر info لكل طلب، فلا ادعاء تغطية كل أنواع السجلات.

| المصدر | النتائج |
|---|---|
| API الإنتاج | 11 handled invalid_credentials؛ لا خطأ آخر في error/warn المسترجعة |
| عامل الإنتاج | خطأ اتصال المصادقة السابق الواحد؛ 35 سجل job completed ذي عمل فعلي: housekeeping=24 وmark-missed=11 |
| خدمة المعاينة API/worker | 371 error/warn في أربع صفحات: ENOENT=141، not_found=141، token_expired=67، invitation_invalid=8، invalid_credentials=6، forbidden=5، consent_required=1، high_risk_confirmation_required=1، فشل اتصال=1 |
| نجاحات عامل المعاينة ذات عمل فعلي | 32: housekeeping=12، mark-missed=9، reminders=5، dispatch=5، stock-alerts=1 |

**N25 — مؤكدة جزئيًا، P2:** ENOENT متكرر في معالجة طلبات المعاينة؛ الوجود مثبت بالسجل، ولم يُثبت بعد المسار الجذري أو أنه يصيب رحلة مريض صحيحة. يلزم إعادة إنتاج طلب اصطناعي محدد ومقارنته بحدود web fallback قبل الإصلاح. لا تُفسّر كل 404 أو رفض إذن بأنه عيب.

## F4 وF7 وF8 وF17

**F4:** تأكد الربط الآن من واجهة EAS الحية، لا من الوثيقة القديمة وحدها. بناء `0.1.0 (6)`، ID `30c14b62-1c98-45ee-9c2c-4c186c7a043f`، ناجح وcommit **63b5b8d33a502b8e3d3cd2ca94b1855266665f90**. بناء `0.1.0 (10)`، ID `a9038762-3554-4f76-9b2f-56a3a2930145`، ناجح وcommit e59. لم تُغيّر مجموعة TestFlight أو حالة توزيع أو إعداد Apple.

**F8:** نُزّل IPA10 من رابط artifact في صفحة EAS وفُحص ZIP وplist محليًا. الحجم **17330712 bytes**؛ SHA-256 **063e88a1845e52d96215dd029635c7e598d618d5f430e11ab84ee596e38baea7**. Info.plist يؤكد build=10 وversion=0.1.0. يوجد **GoogleService-Info.plist واحد في جذر حزمة التطبيق**، BUNDLE_ID مطابق للتطبيق، وGOOGLE_APP_ID/BUNDLE_ID/PROJECT_ID/API_KEY موجودة وغير فارغة؛ لم تُطبع قيمها. إذن **فجوة إثبات وجود الملف حُسمت، ولا يوجد دليل على عيب غيابه**؛ لا تعديل ملف/سر لهذا البند. نجاح توثيق الهاتف على جهاز ما زال «تعذّر التحقق». التقرير الأصلي لم يجزم بالغياب؛ لا ننسب إليه هذا الادعاء. تعذر مسار تنزيل HTTP المباشر ثم اكتمل تنزيل المتصفح؛ فحص الملف المحلي هو الدليل، وليس رسالة واجهة التنزيل.

**F7:** القيمة الفعلية لـPUBLIC_APP_URL **تعذّر قراءتها**: الموصل المتاح يوفر تعديل env ولا يوفر قراءتها، وصفحة Render تحتاج تسجيل دخول. لم نستخدم عملية تحديث للحصول على قيمة، ولم نغيّر إعدادًا. مصدر القيمة الافتراضية لا يثبت القيمة الفعلية أو ملكية النطاق. يبقى الحكم «مؤكدة جزئيًا» بشأن نقص الحارس في المصدر فقط.

**F17:** اتصال app/worker المحدود وخصائص الأدوار مثبتة كما سبق؛ اسم المستخدم داخل DATABASE_URL الفعلي ووجود owner credentials في بيئة API **تعذّر التحقق منهما** للسبب نفسه. يبقى الحكم «مؤكدة جزئيًا». لا يُقترح تغيير أسرار ضمن هذا التفويض.

## البوابات والآثار الجانبية

لا إصلاح أو نشر جديد في هذه المرحلة. لا backup حديث مؤكّد لهذه الجولة، ولا rollback مُجرّب لهذه التغييرات التي لم تُكتب بعد. مقارنة commits المعاينة/الإنتاج غير متطابقة. لذا هذه الأدلة **لا تمنح بوابة نشر إنتاج**.

الآثار الخارجية: طلبات GET للصحة والنسخة؛ استعلامات SQL للقراءة فقط؛ قراءة metadata وسجلات Render؛ قراءة EAS وتنزيل IPA. قد تُسجل تلك القراءات في سجلات المزود وقد يوقظ GET خدمة معاينة نائمة؛ لم يُستدع تشغيل عامل/تنظيف أو إرسال رسالة. أعمال العامل الموجودة في السجلات أعمال الخدمة الجارية، وليست أوامر أُطلقت في هذا الفحص. لا حذف/تعديل بيانات منشورة، ولا حسابات اختبار بالإنتاج، ولا إرسال بريد أو push أو دعوات، ولا مفاتيح أو إعداد Apple جديدة.

يستمر الإصلاح المحلي وPRs للبنود المثبتة رغم حدود قراءة env/قاعدة المعاينة. التحقق المطلوب لصلاحية النشر يبقى بوابة صريحة، ولا تُملأ فجواته بالتخمين.
