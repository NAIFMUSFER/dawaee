# تجهيز تجربة الهاتف — 16 سبتمبر 2026

أذن المستخدم بنشر الاختبار وبناء Android، وأكد مساحة Render `tea-d9qth1iju40c73btab90`. لا يشمل ذلك الإنتاج أو main أو رسائل لأشخاص حقيقيين.

- خدمة الاختبار فقط: `srv-daipkbuk1f9s73952trg`، `https://dawaee-audit-preview.onrender.com`؛ PostgreSQL17 مستقل `dpg-daipq80jo6nc73fsmhhg-a` / `dawaee_audit_db`.
- فرع الاختبار كان4265c787؛ أُثبت أنه ancestor للنسخة المختبرةac4a7c3 قبل تحديثه دون force. النشر التلقائي `dep-dalebk1srm7s73crc70g` فشل: مجلد node_modules المخزن لا يحتوي إضافة Firebase الجديدة. لم يصل إلى بدء الترحيلات.
- إصلاح السبب: تثبيت `npm ci --legacy-peer-deps` داخل hook بناء الويب الخاص بخدمة الاختبار، قبل Expo. لا يكفي وجود node_modules. انحدار يشغّل hook في دليل معزول مع مجلد قديم يثبت ترتيب التثبيت/التصدير ويثبت توقف التصدير عند فشل التثبيت؛ 4 اختبارات hook محلية PASS.
- EAS whoami أعاد Not logged in. البديل مسار GitHub Actions `audit-android.yml` بقراءة المستودع فقط، بلا أسرار أو مفاتيح إنتاج: يولّد APK تجريبيًا مستقلًا `app.dawaee.audit` ويثبت هوية الحزمة والتوقيع وSHA256 وcommit المصدر. ليس AAB للمتجر، ولا يعدّل مسار أدلة بناء الإنتاج السابق. رابط artifact ونتيجة النسخة المنشورة يُسجلان في PR بعد الاكتمال.
- APK مخصص لـarm64-v8a وarmeabi-v7a، باسم «تداوي | TADAWEE تجريبي»، API الاختبار فقط. يثبت بجانب التطبيق الأساسي، ولا يشاركه جلسة أو قاعدة محلية أو إشعارات مجدولة.

## حدود لا تغلقها هذه التجربة

Firebase غير مسجل لحزمةaudit؛ SMS واستعادة كلمة المرور بإثبات مزود فعلي **BLOCKED**. يلزم مشروع/عميل Firebase اختباري مناسب وتوقيع مسجل ثم اختبار الهاتف؛ لا يُعاد استعمال إعداد الإنتاج ولا تزوير إثبات. Push/OCR من مزود وهمي والتخزين محلي في بيئة الاختبار. خدمةRender المجانية قد تنام ولا تثبت دوام العامل أو التنبيهات بالخلفية، والملفات المحلية لا تثبت الاستمرارية بعد إعادة التشغيل. استخدم بيانات مصطنعة فقط. نجاح رابط الويب ليس دليل لمس أو تمرير أو وصول إشعار على هاتف.

## قبول الهاتف

1. ثبّت APK التجريبي وأنشئ حساب اختبار منفصلًا، دون بيانات صحية أو صور أشخاص.
2. أضف دواء بكمية6 ووحدة مناسبة وأربعة مواعيد؛ افتح الوقت وأغلقه وجرّب الرجوع والتمرير ولوحة المفاتيح وتكبير الخط وأيام الأسبوع السبعة.
3. احفظ وافتح التطبيق ثانية؛ تحقق من الكمية والمواعيد. عدل موعدًا واحذفه وجرّب منع تكراره.
4. أكد جرعة مع مخزون ثم تراجع وأكد ثانية؛ راجع الرصيد والسجل بعد التحديث. جرّب انقطاع الاتصال والنقر المكرر.
5. سجل الهاتف وإصدارAndroid وطريقة التنقل واللغة وcommitAPK، وصنّف كل حالة PASS/FAIL/BLOCKED/NOT VERIFIED. لا تحسب شاشة عدم تهيئةSMS اختبارًا ناجحًا للاستعادة.

## التراجع

لا حذف لبيانات الاختبار أو إيصالات الاستعادة. احتفظ بسجل النشر السابق، ولا ترجع خادمًا قديمًا إلى مخطط جديد دون تحقق التوافق. عند تعذر النشر يبقى الإصدار السابق live؛ لا تضع علامة PASS على رابط قديم. الإنتاج لم يُحدّث بهذه العملية.
